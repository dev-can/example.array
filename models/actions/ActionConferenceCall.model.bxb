action (ActionConferenceCall) {
  description (__DESCRIPTION__)
  type (Search)
  collect {
    input (contacts) {
      type (StructContact)
      min (Required) max (Many)
    }
  }
  output (viv.core.Text)
}
